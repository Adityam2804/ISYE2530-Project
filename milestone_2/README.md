# Milestone 2 — Data Cleaning and Database Formation

## Goal
Transform the dataset approved in Milestone 1 into a trustworthy relational SQLite database.

## Dataset
Milestone 2 uses the same approved file:

```text
../milestone_1/dataset.xlsx
```

Do not copy, rename, or replace the dataset for Milestone 2.

## Student responsibilities
Complete the TODO functions in `src/` and the required SQL in `sql/`.
The instructor-provided `main.py` guides execution through loading, inspection, cleaning, relational table creation, validation, SQLite creation/loading, database validation, and SQL checks.
